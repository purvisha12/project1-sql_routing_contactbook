<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <% result.forEach(function(ejs_user){ %>

        <form action="/up/<%= ejs_user.id %>" method="post">

            Name: <input type="text" value="<%= ejs_user.name %>" name="name"><br><br>
            contact_no: <input type="text" value="<%= ejs_user.contact %>" name="contact"><br><br>
            <input type="submit" value="update" name="save">
        </form>
        <% }) %>
    </body>
</html>